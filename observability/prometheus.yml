global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "pipeline-metrics"
    static_configs:
      - targets: ["pipeline:8000"]
        labels:
          app: "ecommerce-pipeline"
